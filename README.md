# Geo-QR-Attendance

This repository contains the **web frontend** for the Geo QR Attendance system.

All web code lives inside the `website/` folder.

---

## 🚀 Run locally

```bash
cd website
pnpm install
pnpm dev